import { RouteFormatDirective } from './route-format.directive';

describe('RouteFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteFormatDirective();
    expect(directive).toBeTruthy();
  });
});
